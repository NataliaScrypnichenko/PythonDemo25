# # SET-не має індекса
# # Найчастіше використовується щоб зібрати дублікати
# l=[1,2,3,1,2,5,1,3,6,7,4,5,6,7]
# s=set(l)
# print(s)
# # s=list(set(l))-перетворюємо в список і можна працювати
# # 📌 set (множина) — це невпорядкована колекція унікальних елементів.
# # У set не може бути дублікатів та немає індексiв.
# # ✅ Створення set
# міста = {"Київ", "Львів", "Одеса"}
# print(міста)  # {'Львів', 'Одеса', 'Київ'}
# # ✅ Особливість set – унікальні значення
# цифри = {1, 2, 3, 3, 2, 1}
# print(цифри)  # {1, 2, 3}
# # 📌 Дублікати автоматично видаляються.
# # ✅ Створення порожньої множини
# порожній_сет = set()  # Правильний спосіб
# print(type(порожній_сет))  # <class 'set'>
# # ❌ НЕ використовуй {} – це створить порожній словник (dict).
#
# # 🔹 Основні операції з set
# # ✅ Додавання елементів
# міста.add("Харків")
# print(міста)  # {'Київ', 'Львів', 'Одеса', 'Харків'}
#
# # ✅ Видалення елементів
# міста.remove("Львів")  # Якщо елемента немає – буде помилка
# міста.discard("Львів")  # Якщо елемента немає – помилки не буде
# міста.pop()  # Видаляє випадковий елемент-витягує випадковий елемент
# міста.clear()  # Очищає весь set
# міста.update() #
#
# # 🔹 Операції над множинами
# # ✅ Об'єднання (| або union())
# A = {1, 2, 3}
# B = {3, 4, 5}
#
# print(A | B)  # {1, 2, 3, 4, 5}
# print(A.union(B))  # {1, 2, 3, 4, 5}
#
# # ✅ Перетин (& або intersection())/все що є спільне
# print(A & B)  # {3}
# print(A.intersection(B))  # {3}
#
# # ✅ Різниця (- або difference())/того чого не має в другому сеті
# print(A - B)  # {1, 2}
# print(A.difference(B))  # {1, 2}
#
# # ✅ Симетрична різниця (^ або symmetric_difference())\те що не співпадають значення
# print(A ^ B)  # {1, 2, 4, 5} (всі, окрім спільних)
# print(A.symmetric_difference(B))  # {1, 2, 4, 5}

# перевіряється чи  є сурерсет для іншого сета,тобто  перевіряє чи є всі ті
# значення всеті який ми перепровіряємойого
s={1, 2, 3}
s2={1, 2, 3, 4}
print(s2.issuperset(s))
print(s.issubset(s2)) # зворотній варіант,чи s  є підсетом s2
print(s2.isdisjoint(s))# чи є спільні єлементи
s.update(s2)
# 🔹 Висновок
# ✅ set – це колекція унікальних елементів.
# ✅ Використовується для швидких перевірок належності (in).
# ✅ Дозволяє виконувати математичні операції над множинами (об'єднання, перетин тощо). 🚀
