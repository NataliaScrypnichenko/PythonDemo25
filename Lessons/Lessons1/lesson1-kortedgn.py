# tuple=# називають чуплами. /тапл або кортеж/це список сконстантний тоб то список, який не можна змінювати
# вони використовуються для скорочення памяті і також будемо отримувати їх на бекенді
# Кортеж (tuple) — це незмінний упорядкований набір значень у Python.
# На відміну від списку (list), кортеж не можна змінювати (додавати, видаляти або змінювати елементи після створення).
# ✅ Синтаксис створення кортежу:
# my-tuple=(1,2,3)
# 📌 Кортежі оголошуються в круглих дужках (), тоді як списки (list) використовують квадратні [].
# ✅ Приклади створення кортежів
# 🔹 Кортеж з числами
numbers = (10, 20, 30)
print(numbers)  # (10, 20, 30)
# 🔹 Кортеж з різними типами даних
mixed = (1, "hello", 3.14, True)
print(mixed)  # (1, 'hello', 3.14, True)
# 🔹 Порожній кортеж
empty_tuple = ()
print(empty_tuple)  # ()
# 🔹 Кортеж з одним елементом (важливо!)
one_element_tuple = (5,)  # Потрібна кома, інакше це не буде кортежем!
print(one_element_tuple)  # (5,)
# 📌 Якщо пропустити кому, то Python сприйме це як звичайне число!
# ✅ Доступ до елементів кортежу
# Так само, як у списку, ми можемо звертатися до елементів за індексом.
numbers = (10, 20, 30)
print(numbers[0])  # 10
print(numbers[1])  # 20
print(numbers[-1])  # 30 (останній елемент)
# ✅ Незмінність кортежу (immutability)
# Кортеж не можна змінювати після створення:
numbers = (10, 20, 30)
numbers[1] = 100  # ❌ Це викличе помилку!
# ❌ Помилка:
# TypeError: 'tuple' object does not support item assignment
# 📌 Якщо потрібно змінити кортеж — створіть новий!
numbers = (10, 20, 30)
new_numbers = numbers[:1] + (100,) + numbers[2:]
print(new_numbers)  # (10, 100, 30)
# ✅ Перетворення list в tuple і навпаки
# 🔹 Перетворення списку в кортеж
my_list = [1, 2, 3]
my_tuple = tuple(my_list)
print(my_tuple)  # (1, 2, 3)
# 🔹 Перетворення кортежу в список
my_tuple = (1, 2, 3)
my_list = list(my_tuple)
print(my_list)  # [1, 2, 3]

# ✅ Основні операції з кортежами
# 🔹 Об'єднання (конкатенація)
a = (1, 2, 3)
b = (4, 5)
c = a + b
print(c)  # (1, 2, 3, 4, 5)
# 🔹 Повторення
numbers = (1, 2) * 3
print(numbers)  # (1, 2, 1, 2, 1, 2)
# 🔹 Перевірка наявності елемента (in)
fruits = ("яблуко", "банан", "вишня")
print("банан" in fruits)  # True
print("груша" in fruits)  # False

# ✅ Методи кортежів
# Оскільки кортеж незмінний, він має всього два методи:
#
# Метод	Опис
# count(значення)	Підраховує, скільки разів значення зустрічається у кортежі
# index(значення)	Повертає індекс першого входження значення
# 🔹 Приклади використання методів
numbers = (1, 2, 3, 2, 4, 2)

print(numbers.count(2))  # 3 (кількість двійок)
print(numbers.index(3))  # 2 (індекс числа 3)
# 🟢 Коли використовувати tuple замість list?
# ✅ Якщо дані не повинні змінюватися (наприклад, дні тижня, кольори).
# ✅ Кортежі займають менше пам’яті, ніж списки, тому їх використання може покращити продуктивність.
# ✅ Кортежі швидші, ніж списки, при ітерації та доступі до елементів.
# ✅ Кортежі можуть використовуватися як ключі у словниках, бо вони незмінні.
#
# 🟢 Висновок
# 📌 Кортеж (tuple) — це незмінний список, який використовує круглі дужки ().
# 📌 Елементи доступні за індексами, але не можна змінювати існуючі значення.
# 📌 Використовується там, де потрібна стабільність і економія пам’яті.
#
# Тепер ти знаєш, як працює tuple у Python! 🚀😊

