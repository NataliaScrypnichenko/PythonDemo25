# dictionary-це словники,словники якщо порівняти із джеесом, то це об'єкти
# dict-це зарезервоване слово ф-я,яка перетворює щось,пишеться в душках {}
# 📌 Словник (dict) — це невпорядкована структура даних, яка зберігає пари "ключ – значення".
# Ключі у словнику унікальні, а значення можуть бути будь-якого типу.
# ✅ Синтаксис створення словника
мій_словник = {
    "name": "Іван",
    "age": 25,
    "city": "Київ"
}
# Ключі – це унікальні ідентифікатори (наприклад, "ім'я", "вік", "місто").
# Значення – це будь-які дані, що відповідають ключам (наприклад, "Іван", 25, "Київ").
# ✅ Створення словника різними способами

# cтворюємо за допомогою ключів
keys=['name', 'age', 'city']
d =dict.fromkeys(keys)
# d =dict.fromkeys(keys, _value:1)
print(d)


# 🔹 Порожній словник
порожній = {}
# 🔹 Словник із dict()
my_dictionary = dict(name="Іван", age=25, city="Київ")
print(my_dictionary)  # {'ім'я': 'Іван', 'вік': 25, 'місто': 'Київ'}

# ✅ Доступ до значень у словнику
# 🔹 За ключем (класичний спосіб)
користувач = {"ім'я": "Оля", "вік": 30}
print(користувач["ім'я"])  # Оля
print(користувач["вік"])   # 30
# 📌 Якщо ключа немає, буде помилка!
#
# 🔹 Отримання значення без помилки (get()
користувач = {"ім'я": "Оля", "вік": 30}
print(користувач.get("місто", "Невідомо"))  # Невідомо
# Якщо ключ не знайдено, get() повертає значення за замовчуванням (а не помилку).

# ✅ Додавання та оновлення значень у словнику
# 🔹 Додавання нового ключа
користувач = {"ім'я": "Оля", "вік": 30}
користувач["місто"] = "Львів"  # Додаємо новий ключ
print(користувач)  # {'ім'я': 'Оля', 'вік': 30, 'місто': 'Львів'}
# 🔹 Оновлення значення існуючого ключа
користувач["вік"] = 31
print(користувач)  # {'ім'я': 'Оля', 'вік': 31, 'місто': 'Львів'}

# Метод update() (одночасно змінює кілька значень)
користувач.update({"вік": 32, "країна": "Україна"})
print(користувач)  # {'ім'я': 'Оля', 'вік': 32, 'місто': 'Львів', 'країна': 'Україна'}
# ✅ Видалення елементів зі словника

# 🔹 Метод pop()
користувач = {"ім'я": "Оля", "вік": 30, "місто": "Львів"}
вік = користувач.pop("вік")
print(користувач)  # {'ім'я': 'Оля', 'місто': 'Львів'}
print(вік)  # 30
# 📌 Видаляє ключ і повертає його значення.

# 🔹 Метод del
del користувач["місто"]
print(користувач)  # {'ім'я': 'Оля'}

# 🔹 Метод clear() (очищення словника)
користувач.clear()
print(користувач)  # {}

# ✅ Перебір словника (for-цикл)
# 🔹 Перебір ключів
користувач = {"ім'я": "Оля", "вік": 30, "місто": "Львів"}
for ключ in користувач:
    print(ключ)  # ім'я,вік,місто
#🔹 Перебір значень
for значення in користувач.values():
    print(значення)  # Оля, # 30 # Львів
#     🔹 Перебір пар "ключ – значення"
for ключ, значення in користувач.items():
    print(f"{ключ}: {значення}")
# 📌 Виведе:
# ім'я: Оля
# вік: 30
# місто: Львів

# ✅ Основні методи словників
# Метод	Опис
# dict.get=(ключ, значення_за_замовчуванням)	Отримує значення за ключем (без помилки, якщо ключ відсутній)
# dict.keys()=Повертає всі ключі словника
# dict.values()=	Повертає всі значення словника
# dict.items()=	Повертає список пар (ключ, значення),кожний елемент буде кортеж- ключ-значення/для літераціїї знадобиться
# dict.pop(ключ)=	Видаляє ключ і повертає його значення
# dict.popitem()=	Видаляє і повертає останню пару (ключ, значення)/видаляє із кінця як були додані ключі
# dict.clear()	=Очищає словник
# dict.update(інший_словник)=	Оновлює словник значеннями з іншого словника
# # 🔹 Приклад використання keys(), values(), items()
користувач = {"ім'я": "Оля", "вік": 30, "місто": "Львів"}
print(користувач.keys())   # dict_keys(['ім'я', 'вік', 'місто'])
print(користувач.values()) # dict_values(['Оля', 30, 'Львів'])
print(користувач.items())  # dict_items([('ім'я', 'Оля'), ('вік', 30), ('місто', 'Львів')])

# ✅ Коли використовувати dict?
# 🔹 Коли потрібно швидко знаходити значення за унікальним ключем.
# 🔹 Коли є структура даних "ключ – значення", наприклад, база користувачів або налаштування.
# 🔹 Коли потрібно зберігати зв’язану інформацію, наприклад, інформацію про продукт.

# 🟢 Висновок
# 📌 Словник (dict) — це структура даних у Python, що зберігає пари "ключ – значення".
# 📌 Ключі унікальні, а значення можуть бути будь-якого типу.
# 📌 Словники швидко шукають значення за ключем і підходять для зберігання структурованих даних.

dictionary={
    "name":"max",
    True:1,
    False:6,
    #[1,2,2,]:44 не може бути
}
# якщо не має такого кдюча дає тещо написано
print(dictionary.get("name1", "nata"))

dictionary. copy()
# — це метод, який створює поверхневу копію словника.
# Він дозволяє створити новий словник, який містить ті ж самі ключі та значення, що й оригінал.
# ✅ Приклад використання copy()
оригінал = {"ім'я": "Оля", "вік": 30, "місто": "Львів"}
копія = оригінал.copy()
print(копія)  # {'ім'я': 'Оля', 'вік': 30, 'місто': 'Львів'}
# 📌 Оригінальний та копійований словники мають однакові дані.
# ✅ Перевірка, що це дві різні змінні
print(оригінал == копія)  # True (значення однакові)
print(оригінал is копія)  # False (це різні об'єкти)
# 📌 Оператор == перевіряє, чи однаковий вміст, а is — чи це один і той самий об'єкт у пам'яті.

# ✅ copy() створює лише поверхневу копію!
# 🔹 Якщо в словнику є вкладені структури (list, dict), то вони не копіюються глибоко.
#
# ❌ Приклад проблеми з вкладеними структурами
оригінал = {"ім'я": "Оля", "вік": 30, "адреси": ["Київ", "Львів"]}
копія = оригінал.copy()
# Змінюємо вкладений список у копії
копія["адреси"].append("Одеса")
print(оригінал["адреси"])  # ['Київ', 'Львів', 'Одеса']
print(копія["адреси"])  # ['Київ', 'Львів', 'Одеса']
# 📌 Проблема: обидва словники посилаються на один і той самий список "адреси", тому зміни впливають на обидва словники!

# ✅ Як зробити "глибоку" копію? (deepcopy)
# Якщо потрібно повністю незалежну копію з вкладеними структурами, використовуй copy.deepcopy().
#
# 🔹 Глибоке копіювання (deepcopy)
import copy

оригінал = {"ім'я": "Оля", "вік": 30, "адреси": ["Київ", "Львів"]}
глибока_копія = copy.deepcopy(оригінал)
# Тепер зміни в одному не впливають на інший
глибока_копія["адреси"].append("Одеса")
print(оригінал["адреси"])  # ['Київ', 'Львів']
# print(глибока_копія["адреси"])  # ['Київ', 'Львів', 'Одеса'
# Тепер "адреси" незалежні у двох словниках.
# 🟢 Висновок
# ✅ copy() створює поверхневу копію словника.
# ✅ Використовується для копіювання словників без зміни оригіналу.
# ✅ При вкладених структурах даних використовуй copy.deepcopy(), щоб уникнути неочікуваних змін. 🚀
# щоб процювати зі списком то конвертуємо йогго в список

# 51:19 закінчила