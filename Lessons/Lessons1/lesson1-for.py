### 🔄 **Цикл `for` у Python**
# `for`=Цикл  використовується для **ітерації** (повторення) по **послідовності** (списку, рядку, множині тощо).
# Він **перебирає** елементи **один за одним**, доки не закінчиться колекція.
## 🔥 **Коли використовувати `for`?**
# ✅ Перебір списків, рядків, словників
# ✅ Фільтрація даних
# ✅ Знаходження мін/макс значень
# ✅ Перетворення списків
# ✅ Обробка файлів (читання рядків)
## 📌 **Синтаксис `for`**
# for змінна in послідовність:
    # Блок коду, який виконується на кожній ітерації
## ✅ **1️⃣ `for` з `range()`**
# Функція `range()` створює послідовність чисел.✅ `range(5)` генерує числа **від 0 до 4** (не включає 5).
for i in range(5):
    print(i)# 🔹 **Виведе:** 0 1 2 3 4

## ✅ **2️⃣ `for` по списку** ✅ `for` бере **кожен елемент списку** та виводить його.
fruits = [" Apple", "Banana", " Cherry"]

for fruit in fruits:
    print(fruit) #🔹 **Виведе:** Apple  Banana Cherry

## ✅ **3️⃣ `for` по рядку** ✅ `for` **перебирає кожну літеру** рядка.
word = "Python"

for letter in word:
    print(letter)#🔹 **Виведе:** P y t h o n

## ✅ **4️⃣ `for` + `enumerate()` (номер + елемент)**✅ `enumerate()` дає **номер + значення**.
names = ["Alice", "Bob", "Charlie"]

for index, name in enumerate(names, start=1):
    print(f"{index}. {name}")#🔹 **Виведе:** 1. Alice 2. Bob 3. Charlie

## ✅ **5️⃣ `for` + `zip()` (перебір кількох списків одночасно)** ✅ `zip()` **поєднує** кілька списків **попарно**.
names = ["Alice", "Bob", "Charlie"]
ages = [25, 30, 35]

for name, age in zip(names, ages):
    print(f"{name} is {age} years old.")
# 🔹 **Виведе:**
# Alice is 25 years old.
# Bob is 30 years old.
# Charlie is 35 years old.

## ✅ **6️⃣ `for` по словнику** ✅ `items()` дає **пари ключ-значення**.
person = {"name": "Alice", "age": 25, "city": "Kyiv"}

for key, value in person.items():
    print(f"{key}: {value}")
# 🔹 **Виведе:**
# name: Alice
# age: 25
# city: Kyiv

## ✅ **7️⃣ `for` + `else`** ✅ Блок `else` виконується **після завершення циклу**.

numbers = [1, 2, 3]

for num in numbers:
    print(num)
else:
    print("Цикл завершено!")# 🔹 **Виведе:** 1 2 3  Цикл завершено!

## ✅ **8️⃣ `for` + `break` (зупинка циклу)** ✅ `break` **зупиняє** цикл.
numbers = [1, 2, 3, 4, 5]

for num in numbers:
    if num == 3:
        print("Знайшли 3! Зупиняємося!")
        break
    print(num)#🔹 **Виведе:** 1 2 Знайшли 3! Зупиняємося!

## ✅ **9️⃣ `for` + `continue` (пропуск ітерації)** ✅ `continue` **пропускає** поточну ітерацію.

for i in range(5):
    if i == 2:
        continue  # Пропускаємо число 2
    print(i)# 🔹 **Виведе:** 0 1 2 3 4

## ✅ **🔟 Вкладені цикли (`for` у `for`)** ✅ Використовується у **матрицях, таблицях, грі "Морський бій" тощо**.

for i in range(3):  # 3 рядки
    for j in range(2):  # 2 колонки
        print(f"({i}, {j})")
# 🔹 **Виведе:**
# (0, 0)
# (0, 1)
# (1, 0)
# (1, 1)
# (2, 0)
# (2, 1)

##  **Швидке порівняння `for` та `while`**
# | Цикл  | Коли використовувати? |
# | **`for`**  | Коли знаєте кількість повторень або перебираєте послідовність (список, рядок). |
# | **`while`** | Коли **умова невідома** заздалегідь і змінюється під час виконання програми. |

