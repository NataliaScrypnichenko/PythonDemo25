# 🔄 **Цикл `while` у Python**

# Цикл `while` використовується, коли **заздалегідь невідомо, скільки разів** потрібно виконати дію.
# Цикл продовжує виконуватись, доки виконується певна **умова**.
# ### 🔥 **Висновок**
# 📌 **Цикл `while` зручний, коли не знаєте, скільки разів потрібно повторювати код**.
# Його часто використовують для **очікування подій, перевірки умов або нескінченного виконання коду**.
#
# Якщо `for` більше підходить для **обробки ітерабельних об'єктів** (списки, рядки, множини),
# то `while` – це про **повторення поки умова істинна**.
# ## 📌 **Синтаксис `while`**
# while умова:
#     # код, який виконується, поки умова ІСТИНА
# ✅ Цикл перевіряє умову **перед кожною ітерацією**.
# ✅ Якщо умова **істинна (`True`)** — виконується код.
# ✅ Як тільки умова стане **хибною (`False`)**, виконання циклу припиняється.

## ✅ **1️⃣ Простий приклад `while`**
count = 0
while count < 5:
    print("Hello, world!")
    count += 1  # збільшуємо лічильник
# 🔹 **Виведе:**
# Hello, world!
# Hello, world!
# Hello, world!
# Hello, world!
# Hello, world!
# ✅ **Як працює код:**
# - `count = 0` →  `count < 5` → друкує `"Hello, world!"` → `count += 1`
# - Так повторюється, поки `count` не стане **5** (умова `count < 5` стане `False`).

## 🔁 **2. Вихід із циклу `while`**Іноді потрібно **достроково зупинити цикл**. Для цього використовують `break` або `continue`.

### 🛑 **Зупинка `while` за допомогою `break`**
count = 0

while count < 10:
    print(count)
    if count == 5:
        break  # Зупиняємо цикл при значенні count = 2
    count += 1
# 🔹 **Виведе:** 0 1 2 3 4 5
# ✅ `break` зупиняє виконання `while`, коли `count == 5`.

### ⏭ **Пропуск ітерації через `continue`**
count = 0
while count < 5:
    count += 1
    if count == 3:
        continue  # Пропускає решту коду в цьому циклі
    print(count)
# 🔹 **Виведе:** # 1 2 3 4 5
# ✅ Коли `count == 2`, викликається `continue`, і **друкування пропускається**.


## ⏳ **Безкінечний `while`-цикл** (обережно!)
while True:
    print("Це безкінечний цикл!")
# ✅ **Цикл не зупиняється самостійно!**
# ❌ Щоб вийти, потрібен `break`, або програма просто зависне.
# Приклад виходу з `while`:
while True:
    answer = input("Введіть 'exit', щоб вийти: ")
    if answer.lower() == "exit":
        print("Вихід з циклу")
        break
# 🔹 Тут цикл **зупиняється лише тоді**, коли користувач вводить `"exit"`.


## 🔁 **3️⃣ `while` з `else`**
# Якщо цикл `while` завершується **нормально** (без `break`), виконається блок `else`:

i = 1
while i <= 3:
    print(i)
    i += 1
else:
    print("Цикл завершено!")
# 🔹 **Виведе:** 1 2 3 Цикл завершено
# ✅ `else` **не виконається**, якщо спрацює `break`.
## ✅ **Приклади використання `while`**

### 🔹 1️⃣ Лічильник з `while`
x = 1
while x <= 5:
    print(f"Крок {x}")
    x += 1
# 🔹 **Виведе:**
# Крок 1
# Крок 2
# Крок 3
# Крок 4
# Крок 5

## ✅ **Зупинка `while` після певного введення**
while True:
    name = input("Введіть ваше ім'я: ")
    if name:
        print(f"Привіт, {name}!")
        break
    else:
        print("Ви не ввели ім'я. Спробуйте ще раз!")
# 🔹 Програма працюватиме, доки користувач не введе хоча б один символ.

## ✅ **Підрахунок суми чисел за допомогою `while`**
total = 0
num = 1

while num <= 5:
    total += num
    num += 1

print(f"Сума чисел від 1 до 5: {total}")
# 🔹 **Виведе:** Сума чисел від 1 до 5: 15

## ✅ **Обхід рядка через `while`**
word = "Python"
i = 0
while i < len(word):
    print(word[i])
    i += 1
# 🔹 **Виведе:** #P y t h o n
# ✅ `while` працює, поки `i` не стане рівним довжині рядка.

## ❌ **Поширені помилки в while**
### 1️⃣ Забули збільшити лічильник → `while` виконується нескінченно
i = 0
while i < 5:
    print(i)
    # пропустили i += 1, тому цикл ніколи не закінчиться!
# ✅ Має бути `i += 1` всередині циклу.
