# #  Оператор if (якщо то)у Python
# a=10
# b=8
# if a<b:
#     print('yes')
# elif a>b:
#     print('no')
# else:
#     print('else')
#
# f=True
# if not f and a>b or a<b:


# if – це умовний оператор, який використовується для прийняття рішень у програмі.
# Він дозволяє виконувати код тільки якщо виконується певна умова.
#
# 📌 Для чого потрібен if?
# Щоб виконати код лише в певних випадках
# Для перевірки умов (наприклад, чи число більше за інше)
# Щоб реагувати на введення користувача
# 🟢 Синтаксис if
# if умова:      # Код виконається, якщо умова True

# ✅ Простий приклад
age = int(input("Скільки тобі років? "))
if age >= 18:
    print("Ти повнолітній!")
# 🔹 Якщо користувач введе 20, то отримає: (Ти повнолітній!)
# 🔹 Якщо введе 15:  нічого не виведеться, бо умова age >= 18 не виконалася.

# 🔹 if...else – альтернатива
# Якщо умова не виконується, можна використати else:
age = int(input("Скільки тобі років? "))

if age >= 18:
    print("Ти повнолітній!")
else:
    print("Ти ще малий!")
# ✅ Якщо користувач введе 20:(Ти повнолітній!)
# ✅ Якщо 15:(Ти ще малий!)

# 🔹 if...elif...else – кілька перевірок
# Якщо є більше ніж 2 варіанти, можна використати elif ("інакше якщо").
age = int(input("Скільки тобі років? "))

if age < 18:
    print("Ти ще дитина!")
elif age < 60:
    print("Ти дорослий!")
else:
    print("Ти пенсіонер!")
#     ✅ Якщо 10:(Ти ще дитина!)
# ✅ Якщо 30:(Ти дорослий!)
# ✅ Якщо 70:(Ти пенсіонер!)

# # ##🔹 Логічні оператори в if
# 🔹 and (i)
# Умова виконується, якщо обидві частини True.
age = int(input("Скільки тобі років? "))

if age >= 18 and age < 60:
    print("Ти працюєш?")
#     ✅ Якщо 25, виведе: Ти працюєш?
# ✅ Якщо 65, нічого не вивед

# 🔹 or (АБО)
# Умова виконується, якщо хоча б одна частина True.
a=(дощить) = True
b=(є_парасолька) = False

if a or b:
    print("Візьми парасольку!")
# ✅ Виведе "Візьми парасольку!", бо дощить.

# 🔹 not (НЕ)
# Перетворює True у False, і навпаки.заперечення
c=(світло_є) = False

if not c:
    print("Світло відключили! 💡")
# ✅ Виведе "Світло відключили! 💡", бо not False → True.

# ##🔹 Вкладені if (перевірка всередині перевірки)
age = int(input("Скільки тобі років? "))
s = ('якщо-навчається')

if age >= 18:
    print("Ти повнолітній!")
    s = input("Ти студент? (так/ні): ")

    if s == "так":
        print("Вау, студентське життя цікаве!")
    else:
        print("Ти вже працюєш?")
else:
    print("Ти ще дитина!")

#     🔹 if у скороченому записі
age = 20
b=('повідомлення-')
b = "Повнолітній" if age >= 18 else "Неповнолітній"
print(b)
# 🟢 Висновок
# 📌 if використовується для прийняття рішень.
# 📌 else виконується, якщо if не виконався.
# 📌 elif додає додаткові умови.
# 📌 and, or, not допомагають створювати складні перевірки.
# 📌 if можна вкладати один в один.



# Ось приклад вкладеного if для роботи з числами. Програма запитує у користувача число та перевіряє його категорію:
число = int(input("Введи число: "))

if число > 0:
    print("Це додатнє число!")

    if число % 2 == 0:
        print("Також воно парне.")
    else:
        print("Але воно непарне.")

elif число < 0:
    print("Це від’ємне число!")

    if число % 2 == 0:
        print("Також воно парне.")
    else:
        print("Але воно непарне.")

else:
   print("Це нуль!")

# 🔹 Як використати %?
# 1️⃣ Перевірка парності числа
# Якщо число % 2 == 0, то число парне, інакше непарне.
число = int(input("Введи число: "))

if число % 2 == 0:
    print("Це парне число!")
else:
    print("Це непарне число!")
# 2️⃣ Перевірка кратності числа
# Якщо a % b == 0, значить a ділиться на b без залишку
число = int(input("Введи число: "))

if число % 5 == 0:
    print("Число кратне 5!")
else:
    print("Число не кратне 5!")

# 🔹 Тернарний оператор в Python
num=int(input('Enter a number: '))
res= 'yes' if num>5 else 'no'
print(res)
# Тернарний оператор – це скорочений спосіб запису if...else, який використовується для простих умовних виразів в один рядок.
# # ✅ Синтаксис:
# результат = значення_якщо_True if умова else значення_якщо_False
# 🔹 Приклад 1: Перевірка числа (додатне/від’ємне)
number = int(input("Введи число: "))
res = "Додатнє" if число > 0 else "Від’ємне або нуль"
print(res)
# 🔹 Приклад 2: Перевірка парності числа
число = int(input("Введи число: "))
тип_числа = "Парне" if число % 2 == 0 else "Непарне"
print(тип_числа)
# 🔹 Приклад 3: Максимальне з двох чисел
a = 10
b = 20

max = a if a > b else b
print("Максимальне число:", max)
# 🔹 Приклад 4: Скорочена перевірка віку
вік = int(input("Введіть свій вік: "))
доступ = "Дозволено" if вік >= 18 else "Заборонено"
print(доступ)
# 🟢 Висновок
# 📌 Тернарний оператор допомагає скорочувати код, замінюючи if...else одним рядком.
# 📌 Його варто використовувати для простих умов, щоб зробити код читабельнішим.
# 📌 Якщо умова складна – краще використовувати стандартний if...else.



